# ── Database ──
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=WayHax

# ── Ports (можно менять если заняты) ──
DB_PORT=5432
BACKEND_PORT=8000
FRONTEND_PORT=3000

# ── API Keys ──
GEMINI_API_KEY=
